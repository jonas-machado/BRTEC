import { UpdateOptions } from "react-toastify";

export const toastUpdate: UpdateOptions = {
  autoClose: 3000,
  isLoading: false,
  pauseOnHover: false,
  theme: "dark",
};

export const toastInfo: UpdateOptions = {
  isLoading: false,
  pauseOnHover: false,
  theme: "dark",
};
